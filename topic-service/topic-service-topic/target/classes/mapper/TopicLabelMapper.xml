<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ey.topic.topic.mapper.TopicLabelMapper">

    <select id="getLabelNamesByIds" resultType="java.lang.String">
        SELECT label_name
        FROM topic_label
        WHERE id IN
        <foreach collection="labelIds" open="(" item="id" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>