<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ey.topic.topic.mapper.TopicDailyStagingMapper">

    <insert id="insertBatch">
        INSERT INTO topic_daily_staging
        (user_id,topic_id, subject_id, is_public) VALUES
        <foreach collection="insertList" item="item" separator=",">
            (#{item.userId},#{item.topicId},#{item.subjectId},#{item.isPublic})
        </foreach>
    </insert>
</mapper>